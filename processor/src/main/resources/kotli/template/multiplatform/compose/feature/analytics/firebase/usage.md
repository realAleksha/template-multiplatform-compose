This implementation uses [Firebase Kotlin SDK](https://github.com/GitLiveApp/firebase-kotlin-sdk).

Make sure you have `google-services.json` (for Android) and `GoogleService-Info.plist` (for iOS) configured in your project.
